FROM scratch

ARG TARGETARCH
ARG BINARY=dist/modary-rulary-linux-${TARGETARCH}
COPY ${BINARY} /modary

VOLUME ["/data"]
EXPOSE 8080

ENTRYPOINT ["/modary"]
CMD ["serve", "--listen", "0.0.0.0:8080", "--db", "/data/modary.db"]
