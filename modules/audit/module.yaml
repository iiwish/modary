schemaVersion: modary.module/v1alpha1
id: audit
version: 0.1.0
type: feature
requires:
  - database
  - authorization
provides:
  - audit
actions:
  - audit.query
migrations:
  sqlite: migrations/sqlite
ui:
  routes:
    - id: audit.logs
      path: /audit
      entry: ./ui/routes.ts
